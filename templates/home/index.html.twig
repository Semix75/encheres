{% extends 'base.html.twig' %}

{% block title %}Accueil | Enchères Inversées{% endblock %}

{% block body %}
<div class="container text-center mt-5">
    <h1 class="display-4 text-primary">🎯 Bienvenue sur <strong>Enchères Inversées</strong> !</h1>
    <p class="lead text-muted">Misez intelligemment, dépensez moins 💸</p>

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <div class="alert alert-success mt-4">
            👋 Bonjour <strong>{{ app.user.nom }}</strong> ! Ravi de vous revoir.
        </div>

        <div class="d-flex justify-content-center gap-3 mt-4">
            <a href="{{ path('app_dashboard') }}" class="btn btn-primary btn-lg">
                🧾 Accéder aux enchères
            </a>
            <a href="{{ path('app_mes_mises') }}" class="btn btn-outline-dark btn-lg">
                📜 Mes mises
            </a>
            <a href="{{ path('app_logout') }}" class="btn btn-danger btn-lg">
                🔒 Déconnexion
            </a>
        </div>
    {% else %}
        <div class="d-flex justify-content-center gap-3 mt-5">
            <a href="{{ path('app_login') }}" class="btn btn-success btn-lg">
                🔑 Se connecter
            </a>
            <a href="{{ path('app_register') }}" class="btn btn-primary btn-lg">
                📝 S'inscrire
            </a>
        </div>
    {% endif %}
</div>
{% endblock %}
